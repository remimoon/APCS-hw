import java.io.*;
import java.util.*;

public class Driver {
	static ArrayList<Integer> ai = new ArrayList<Integer>();

    public static void random(){
	Random r = new Random();
		for (int i = 0; i < 10; i++){
		    ai.add(r.nextInt(5));
		}
		System.out.println(ai.sort);
    }
